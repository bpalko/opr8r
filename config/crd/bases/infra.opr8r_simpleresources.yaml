apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: simpleresources.infra.opr8r
spec:
  group: infra.opr8r
  names:
    kind: SimpleResource
    listKind: SimpleResourceList
    plural: simpleresources
    shortNames:
    - simple
    singular: simpleresource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.length
      name: Length
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.randomValue
      name: RandomValue
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SimpleResource is the Schema for the simpleresources API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.'
            type: string
          metadata:
            type: object
          spec:
            description: SimpleResourceSpec defines the desired state of SimpleResource
            properties:
              length:
                description: Length of the random string to generate
                maximum: 100
                minimum: 1
                type: integer
              prefix:
                description: Prefix for the generated output file
                type: string
              vars:
                additionalProperties:
                  type: string
                description: Vars contains additional Terraform variables as key-value
                  pairs
                type: object
            required:
            - length
            type: object
          status:
            description: SimpleResourceStatus defines the observed state of SimpleResource
            properties:
              filePath:
                description: FilePath is the path to the created file
                type: string
              lastAppliedHash:
                description: LastAppliedHash tracks the hash of the last applied spec
                type: string
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              phase:
                description: 'Phase represents the current phase of the resource Possible
                  values: Pending, Applying, Ready, Error, Destroying'
                type: string
              randomValue:
                description: RandomValue is the generated random string
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
